<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyx.dao.BugTicketDao">



    <!--查询所有缺陷追踪表-->
    <select id="findAll" resultType="com.lyx.entity.BugTicket">
        select bug_id,team_id,title,description,status_code,status_name,bug_level,create_id,create_name,create_time,submit_id,submit_name,submit_time,check_id,check_name,check_time,deal_id,deal_name,deal_time,test_id,test_name,test_time
        from t_bug_ticket
    </select>

    <!--保存缺陷追踪表-->
    <insert id="save" parameterType="com.lyx.entity.BugTicket" useGeneratedKeys="true" keyProperty="bug_id">
        insert into t_bug_ticket values(#{bug_id},#{team_id},#{title},#{description},#{status_code},#{status_name},#{bug_level})
    </insert>

    <!--根据id删除缺陷追踪表-->
    <delete id="delete" parameterType="String">
        delete from t_bug_ticket where bug_id = #{bug_id}
    </delete>


    <!--更新缺陷追踪表-->
    <update id="update" parameterType="com.lyx.entity.BugTicket">
        update t_bug_ticket set team_id=#{team_id},title=#{title},description=#{description},status_code=#{status_code},status_name=#{status_name},bug_level=#{bug_level}
        where bug_id = #{bug_id}
    </update>


</mapper>